import { View, Text } from 'react-native';

// TODO Phase 2: react-native-maps + Supabase pins
export default function MapTab() {
  return (
    <View className="flex-1 bg-[#0d2b3e] items-center justify-center">
      <Text className="text-[#eef6fb] text-xl">Pins Map — Phase 2 migration pending.</Text>
    </View>
  );
}
