import { View, Text } from 'react-native';

// TODO Phase 2: Gallery with react-native image pipeline
export default function GalleryTab() {
  return (
    <View className="flex-1 bg-[#0d2b3e] items-center justify-center">
      <Text className="text-[#eef6fb] text-xl">Gallery — Phase 2 migration pending.</Text>
    </View>
  );
}
